# 10.13.25-safe.3 实验性完整便携包：运行前必读

适用于 Windows x64。内含 .NET 8 运行时，不需要另装 .NET；仍需 Microsoft Edge WebView2 Runtime。主程序未进行代码签名，启动按原项目要求请求管理员权限；请核对发布来源及 SHA-256，不要为运行它关闭系统安全功能。

## 首次部署

1. 保存工作，结束游戏并退出 MSI Afterburner、其他硬件调参工具。先从托盘完全退出旧版蛟龙控制中心，否则单实例检查会阻止新版启动。
2. 将整个 ZIP 解压到一个新的、可写目录。不要只复制 EXE；不要覆盖旧版目录，也不要复制旧版 `config/` 或开启开机自动应用。
3. 启动 `JiaoLongControl.exe`。先检查页面显示和只读检测。这个发布包不附带个人配置，不自动写入实验性 GPU 曲线。
4. GPU 页面先“读取曲线”，然后点击“生成 4060 保守预设（仅预览）”，或手动选择电压锚点/目标频率。预设采用本机 900 mV 点的原有频率，不提升任何曲线点；本机实测为 2250 MHz，其他机器以自身读回为准。暂时不要同时修改 CPU CO、功耗和 GPU 曲线。没有适用于所有 4060 Laptop 的通用稳定参数。

## CPU 与 PawnIO

CPU 基本设置使用 OEM / Windows 接口，兼容性取决于机型；SMU / CO 仍然需要 PawnIO 内核驱动。内置官方签名安装器的文件/产品版本是 **2.2.0.0**，并非 3.1.0。已安装时先查看状态，不必重复安装。只有在设置页明确确认后才打开交互式安装器；请选择官方签名版，不选 Unrestricted。此包不会替你安装驱动。

## GPU 试用与恢复

- 曲线功能是实验性私有 NVAPI 路径，safe.3 已在 RTX 4060 Laptop / 616.64 实测整段 900 mV / 2250 MHz 平台（54 个点发生变化），独立读回、手动恢复及后端 60 秒超时恢复均通过。**未验证长时间游戏稳定性、功耗收益或系统死机恢复**。读取失败或提示不支持时停止，不要绕过检查。
- 只有明确确认并点击“试用 60 秒”才尝试写入。成功读回后可确认保留本次会话；正常退出仍尝试恢复原偏移。备份位于 `%LOCALAPPDATA%/JiaoLongControl/curve-backup-v1.json`。
- 计时器不能保证在驱动挂起、黑屏、系统崩溃、强制结束进程时工作。再次打开程序时，保留备份并按页面提示恢复；驱动或设备变化时不会强套旧备份。
- 回到旧版前，先在新版恢复曲线并确认成功，再退出新版、打开旧版。仅换回旧 EXE 不等于恢复硬件。不要删除恢复备份来绕过应用门槛。
- safe.2 使用修正后的 255 槽控制表和备份内容版本 2；恢复文件路径保留不变。safe.1 的旧格式备份会被拒绝，不会猜测转换或自动删除。若有旧备份，请保留并停止调参。

## 显存档位修复

safe.2 错把相同的显存基础/Boost 频率当作上下限，导致 8001～8001 MHz 滑块不能移动。safe.3 改用驱动报告的离散档位，本机为 405、810、6001、7001、8001 MHz。它不是小飞机的 +MHz 显存超频偏移；低档位会明显降低性能，默认保留标称输入。仅发现档位不代表驱动允许 `-lmc` 写入；若驱动拒绝，页面保留错误，不保存失败值。本次实机测试只写了核心曲线，没有测试显存锁频写入。

本分支已关闭上游自动更新。开发期间未改动旧版安装、BIOS、系统启动项、现有驱动或 CPU 参数；用户授权的单点及整段 GPU 曲线测试均已读回确认恢复。开发测试不等于已解决本机黑屏重启；CPU CO 的输入范围也不代表稳定承诺。

完整变更、测试边界、许可和恢复说明见同目录 `SAFE_TUNING.md` 及 `Drivers/PawnIO/SETUP-NOTICE.md`。
